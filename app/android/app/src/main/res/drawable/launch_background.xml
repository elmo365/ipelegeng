<?xml version="1.0" encoding="utf-8"?>
<!--
  The cold-start window. It carries the page colour and nothing else.

  The brand mark belongs here once the artwork exists — see docs/identity.md
  for the asset set the design defines and what is still missing. Until then a
  plain brand-coloured ground is the honest state: a stock glyph would be worse
  than none.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ipelegeLaunchBg" />
</layer-list>
